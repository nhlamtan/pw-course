a. Tạo 3 file fiel1, file2, file3 (Local)
b. Chạy lệnh git init (Local -> Working Directory)
c. Chạy lệnh git commit -m "init project" (Staging Area -> Repository)
d. Chạy lệnh git add file1 (file1 từ Working Directory -> Staging Area)
e. Chạy lệnh git commit -m "add file1" (Staging Area -> Repository)
f. Chạy lệnh git commit -m "file" (Staging Area -> Repository)