//a. Khai báo một hằng số với giá trị kiểu Number bất kỳ
const num = -1;
//b. In ra dòng sau ở code
if (num > 0) {
  // nếu hằng số lớn hơn 0
  console.log("Giá trị bạn nhập là số dương");
}
if (num < 0) {
  // nếu hằng số bé hơn 0
  console.log("Giá tị bạn nhập là số âm");
}
if (num === 0) {
  // nếu hằng số bằng 0
  console.log("Giá trị bạn nhập là số 0");
}
